"""DLSS5Kit - one-click DLSS 5 neural rendering setup for PC games.

A local-first installer: it prefers files already on disk and downloads the
rest from the original publishers. Everything it writes is recorded in a
manifest so uninstall puts the folder back exactly as it was.
"""

__version__ = "1.6.1"
